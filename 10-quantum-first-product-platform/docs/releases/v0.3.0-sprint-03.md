# v0.3.0 — Sprint 3 developer API docs builder

## Delivered

- Tenant-scoped API metadata, endpoint, schema, error, example and quickstart
  resources.
- Independent review before deterministic Markdown generation.
- Immutable documentation builds with SHA-256 provenance.
- Public developer reference and downloadable Markdown without tenant or actor
  identifiers.
- Transactional outbox plus a tenant-assigned delivery worker with leases,
  retries and `SKIP LOCKED` claiming.
- OpenAPI/Swagger, PostgreSQL RLS integration, unit/HTTP/browser acceptance and
  accessibility coverage.

## Evidence boundary

All automated acceptance content is explicitly synthetic. This release does
not assert customer adoption, production API uptime, remote broker delivery,
quantum advantage, security certification or implementation conformance.

## Verification

```powershell
Set-Location "C:\JeanLoa\Path-Software-Engineer\Quantum-First-Product-Platform\10-quantum-first-product-platform"
.\scripts\run-quality-gate.ps1
```
